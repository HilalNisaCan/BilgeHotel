@{
    ViewData["Title"] = "XML Kimlik Bildirim Raporu";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2 class="mb-4 text-primary fw-bold">
    <i class="bi bi-filetype-xml me-2 text-warning"></i> Günlük XML Kimlik Bildirimi
</h2>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success">
        <i class="bi bi-check-circle-fill me-1"></i> @TempData["Success"]
    </div>
}
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">
        <i class="bi bi-x-circle-fill me-1"></i> @TempData["Error"]
    </div>
}

<div class="card shadow border-0 rounded-4 p-4 bg-light text-center">
    <p class="text-muted mb-4">
        Aşağıdaki butona tıklayarak bugün otele giriş yapan müşterilerin bilgilerini XML formatında indirebilirsiniz.
        Bu işlem demo amaçlıdır, valilik sistemine gönderim yapılmaz.
    </p>

    <form asp-action="GenerateTodayGuestXml" method="get">
        <button class="btn btn-outline-primary px-4 fw-bold">
            <i class="bi bi-file-earmark-arrow-down-fill me-1"></i> Bugünkü XML Raporunu İndir
        </button>
    </form>
</div>
