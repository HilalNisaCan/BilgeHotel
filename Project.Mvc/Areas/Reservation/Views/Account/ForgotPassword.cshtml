@model ReservationForgotPasswordViewModel
@{
    ViewData["Title"] = "Şifremi Unuttum";
    Layout = null;
}
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <title>BilgeHotel | Şifremi Unuttum</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />

    <style>
        body {
            background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', sans-serif;
        }

        .login-container {
            background: #fefefe;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.25);
            width: 100%;
            max-width: 420px;
        }

            .login-container h4 {
                margin-bottom: 20px;
                font-weight: bold;
                color: #1c1c1c;
                text-align: center;
            }

        .form-label {
            font-weight: 600;
            color: #333;
        }

        .form-control:focus {
            box-shadow: none;
            border-color: #c49b2e;
        }

        .btn-submit {
            background-color: #c49b2e;
            color: #fff;
            width: 100%;
            transition: all 0.3s ease;
        }

            .btn-submit:hover {
                background-color: #a78624;
            }

        .text-danger {
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h4>🔐 Şifremi Unuttum</h4>

        <form asp-area="Reservation" asp-controller="Account" asp-action="ForgotPassword" method="post">
            <div class="mb-3">
                <label asp-for="Email" class="form-label">E-posta</label>
                <input asp-for="Email" class="form-control" placeholder="E-posta adresiniz" />
                <span asp-validation-for="Email" class="text-danger small"></span>
            </div>

            <button type="submit" class="btn btn-submit">Şifre Sıfırlama Linki Gönder</button>

            <div class="text-center mt-3">
                <a href="/Reservation/Account/Login" class="text-decoration-none">← Giriş Ekranına Dön</a>
            </div>
        </form>
    </div>
</body>
</html>
